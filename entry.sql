CREATE TABLE People (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(255) NOT NULL,
    Age INT,
    Birthdate DATE,
    Gender VARCHAR(50),
    Address VARCHAR(255),
    GmailAccount VARCHAR(255)
);
